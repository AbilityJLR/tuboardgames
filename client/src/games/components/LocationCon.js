import React from "react";
import "./LocationCon.css"

const LocationCon = ({ children }) => {
  return (
    <div className="location-con">
      {children}
    </div>
  )
}

export default LocationCon
